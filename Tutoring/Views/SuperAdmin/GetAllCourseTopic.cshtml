@model IEnumerable<DAL.Entities.tbl_course_topic>

@{
    ViewBag.Title = "GetAllCourseTopic";
    Layout = "~/Views/Shared/_superAdminLayout.cshtml";
}


<div class="card">
    <div class="card-header">
        <h3 class="card-title">All Course Topic</h3><br /><br />
        <a href="@Url.Action("CreateCourseTopic","SuperAdmin")" class="create">
            Create
        </a>
    </div>
    <div class="card-body">
        <table id="example1" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.topicname)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.fk_coursebranchid)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.cr_date)
                    </th>
                    <th>Operation</th>
                </tr>
                </thead>
            <tbody>

                @foreach (var item in ViewBag.data)
                {
                    <tr>
                        <td>
                            @item.topicname
                        </td>
                        <td>
                            @item.fk_coursebranchid
                        </td>
                        <td>
                            @item.cr_date
                        </td>
                        <td>
                            <a href="@Url.Action("UpdateCourseTopic", new { coursetopicid = item.coursetopicid })" class="update">
                                Update
                            </a>
                            <a href="@Url.Action("DeleteClass", new { coursetopicid = item.coursetopicid })" class="delete" onclick="return confirm('Are you sure you wish to delete? This record will no longer exsists.');">
                                Delete
                            </a>
                        </td>
                    </tr>
                }

        </table>
        </div>
</div>